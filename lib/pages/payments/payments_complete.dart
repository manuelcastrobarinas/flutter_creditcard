import 'package:flutter/material.dart';

class PaymentsCompletePage extends StatelessWidget {
  const PaymentsCompletePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('pagos')),
    );
  }
}